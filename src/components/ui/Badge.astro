---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<span class={`inline-flex items-center gap-1.5 px-3 py-1 text-xs font-medium rounded-full border border-white/10 bg-white/5 text-muted-foreground ${className}`}>
  <slot />
</span>
